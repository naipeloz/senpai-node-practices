const API_KEY = "completar codigo KEY"
const BASE_URL = "completar codigo URL"

export const getTemp = () => 
  fetch(`${BASE_URL}"completar codigo URL"${API_KEY}`)
    .then(response => response.json()); 